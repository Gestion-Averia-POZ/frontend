export const API_URL = import.meta.env.VITE_API_URL ?? "";

export const APP_CONFIG = {
  name: "Urbis",
  description: "Sistema de reporte de averías de servicios básicos",
  version: "1.0.0",
} as const;
